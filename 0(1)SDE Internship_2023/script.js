document.addEventListener('DOMContentLoaded', () => {
    const dynamicDataDiv = document.getElementById('dynamic-data');
    const dynamicContent = "This content is dynamically added using JavaScript.";
    dynamicDataDiv.textContent = dynamicContent;
});
